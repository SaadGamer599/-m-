client.on('message', message => {
    if (message.content === 'بوم') {
          let count = 1;
          let ecount = 1;
          for(let x = 1; x < 99999; x++) {
            message.channel.send(`ٍٍٍ`)
              .then(m => {
                count++;
              })
              
            }
          }
    });
    
    
    
    //*npm install anti-spam and npm i discord-anti-spam *
    //البكجات غشان تشغل الكود